/**
 * A module that exports all utils functions
 * @module requests
 */
import parseDate from './parseDate';
import nameInitial from './nameInitial';
import localId from './localId';
import getCurrentYear from './getCurrentYear';

export { 
    parseDate,
    nameInitial,
    localId,
    getCurrentYear
};